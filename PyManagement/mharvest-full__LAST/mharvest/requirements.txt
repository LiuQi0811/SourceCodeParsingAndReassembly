# 核心依赖
requests>=2.28.0
cryptography>=41.0.0

# 可选：blob / MSE 视频抓取（--browser）
#   只有需要抓取 blob: 地址的播放器视频时才装
#   pip install playwright && playwright install chromium
playwright>=1.40.0    # 可选

# 可选：ffmpeg 不是 pip 包，需要系统安装
#   合并 m3u8 的 TS 分片时会用它转封装成 mp4（-c copy，不重编码）。
#   fMP4（.m4s）分片不需要 ffmpeg，直接拼接即可。
#   没装也能跑，只是合并结果保留为 .ts 文件。
#   Ubuntu: sudo apt install ffmpeg
#   macOS:  brew install ffmpeg
