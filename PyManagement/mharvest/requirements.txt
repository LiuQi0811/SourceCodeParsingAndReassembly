# 核心依赖
requests>=2.28.0
cryptography>=41.0.0

# 可选：ffmpeg 不是 pip 包，需要系统安装
#   合并 m3u8 分片时会用它把 ts 转封装成 mp4（-c copy，不重编码）。
#   没装也能跑，只是合并结果保留为 .ts 文件。
#   Ubuntu: sudo apt install ffmpeg
#   macOS:  brew install ffmpeg
