# spiderbuf_async_crawler 依赖
# 核心：Python 3.14.7 + asyncio + aiohttp
aiohttp>=3.9
aiosqlite>=0.20
beautifulsoup4>=4.12
lxml>=5.0
charset-normalizer>=3.3
pycryptodome>=3.20
# 视频合并（需本机 ffmpeg/ffprobe，Windows 可用 gyan.dev 全量构建）
# ffmpeg>=2023  （系统命令，非 pip 包）
