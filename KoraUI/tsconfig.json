{
  "compilerOptions": {
    "target": "ES6",
    // 或更高的 ES 版本
    "module": "ESNext",
    // 使用 ES Modules
    "moduleResolution": "Node",
    // 模块解析策略与 ES Modules 匹配
    "lib": [
      "DOM",
      "ES6"
    ],
    // 关键：包含 "DOM" 才能识别 window、document 等浏览器 API
    "strict": true,
    "esModuleInterop": true,
    "rootDir": "./src",
    // 指定源代码目录为 src（可选，但推荐）
    "outDir": "./dist"
    // 编译后的文件输出到 dist 目录
  },
  "include": [
    "src/**/*.ts",
    "src/types/**/*.d.ts"
  ]
}