import { api, chapter, ex, p, section } from './helpers';

// 第 18 章：worker_threads 与 cluster
export const concurrencyChapter = chapter('concurrency', 'worker_threads / cluster', '多线程与多进程：榨干多核 CPU 的两条路线', [
  section('concurrency-models', '并发模型', [
    api(
      'worker-threads',
      'worker_threads：真正的多线程',
      'new Worker(filename, options?)',
      '在同一个进程内开启多个线程，每个 Worker 拥有独立的 V8 实例与事件循环，是 CPU 密集计算（加密/压缩/大 JSON 解析）不阻塞主线程的官方方案。',
      [
        p('filename', 'string | URL', true, '线程入口文件路径，或 data: 内联脚本'),
        p('options.workerData', 'unknown', false, '随线程启动传入的数据，子线程从 workerData 读取'),
        p('options.execArgv', 'string[]', false, '传给线程的 Node 参数，如 ["--experimental-loader=..."]'),
      ],
      [
        ex('主线程：派生计算任务', [
          "import { Worker } from 'node:worker_threads';",
          '',
          'const worker = new Worker("./hash-worker.ts", {',
          '  workerData: { rounds: 5_000_000 },',
          '});',
          '',
          'worker.on("message", (result: number) => {',
          "  console.log('计算结果:', result);",
          '});',
          '',
          'worker.on("error", (err: Error) => {',
          "  console.error('线程异常:', err.message);",
          '});',
          '',
          'worker.on("exit", (code: number) => {',
          "  console.log('线程退出码:', code);",
          '});',
        ].join('\n'), '计算结果: 12500002500000\n线程退出码: 0'),
        ex('子线程 hash-worker.ts', [
          "import { parentPort, workerData } from 'node:worker_threads';",
          '',
          'function heavyCompute(rounds: number): number {',
          '  let sum = 0;',
          '  for (let i = 0; i < rounds; i++) sum += i;',
          '  return sum;',
          '}',
          '',
          'const result = heavyCompute(workerData.rounds);',
          'parentPort.postMessage(result);',
        ].join('\n')),
      ],
      [
        '每个 Worker 内存隔离：postMessage 走结构化克隆（复制），大数据用 SharedArrayBuffer 共享，避免序列化开销。',
        '只为 CPU 密集而生：I/O 任务用事件循环 + Promise 更省资源，开线程反而是浪费。',
        '手写线程池麻烦，生产推荐 piscina：动态池化 + 任务队列 + 空闲回收开箱即用。',
        'terminate() 立即杀线程且不通知正在执行的任务，仅在超时兜底时使用。',
      ],
    ),
    api(
      'cluster',
      'cluster：多进程负载均衡',
      'cluster.fork()',
      '由主进程派生多个 Worker 进程，所有进程共享监听端口，内核把连接分发给不同进程。进程级隔离 + 自动重启，是 pm2 的核心原理。',
      [
        p('cluster.isPrimary', 'boolean', true, '主进程为 true，Worker 进程为 false，用于分流逻辑'),
        p('os.availableParallelism()', 'number', false, '容器配额内的可用核数，推荐用它决定 fork 数量'),
      ],
      [
        ex('多进程 HTTP 服务', [
          "import cluster from 'node:cluster';",
          "import http from 'node:http';",
          "import os from 'node:os';",
          '',
          'if (cluster.isPrimary) {',
          '  const size = os.availableParallelism();',
          "  console.log('主进程', process.pid, '派生', size, '个 Worker');",
          '  for (let i = 0; i < size; i++) cluster.fork();',
          '',
          '  cluster.on("exit", (worker, code: number) => {',
          "    console.log('Worker', worker.process.pid, '退出 code', code, '，自动重启');",
          '    cluster.fork();',
          '  });',
          '} else {',
          '  http.createServer((req, res) => {',
          "    res.end('served by pid ' + process.pid);",
          '  }).listen(3000);',
          '}',
        ].join('\n'), '主进程 4021 派生 8 个 Worker\nWorker 4023 退出 code 1 ，自动重启'),
      ],
      [
        '所有 Worker 共用 listen 的端口，无需做端口分配；请求分发在 Linux 下由内核抢占式调度。',
        '进程级隔离：一个 Worker 崩溃不影响其他进程，配合 exit 事件自动重启就是最简高可用。',
        'Worker 之间不共享内存：Session 等状态放 Redis，或通过 process.send IPC 广播。',
        'os.availableParallelism()（v18.14+）已适配 cgroups，容器里比 os.cpus().length 更准。',
      ],
    ),
    api(
      'model-compare',
      '并发模型选型',
      '事件循环 / child_process / cluster / worker_threads',
      'Node 的并发工具箱有四件套。选错模型，轻则性能不升反降，重则引入共享状态灾难。判断标准只有一条：瓶颈在不在 JS 主线程的计算上。',
      [],
      [
        ex('四件套能力对比', [
          '事件循环        I/O 密集（网络/磁盘/接口聚合）   默认，零成本',
          'child_process   跑外部命令、强隔离卸载           execFile / spawn / fork',
          'cluster         HTTP 多进程负载均衡              端口共享 + 崩溃隔离',
          'worker_threads  进程内 CPU 密集计算              独立 V8 + 可共享内存',
        ].join('\n')),
      ],
      [
        '并行收益 = 计算耗时 - 线程/进程创建与通信成本。小于 1ms 的任务并行是负优化。',
        'HTTP 多实例部署优先 cluster（或 pm2/k8s 多副本）；单进程内的重计算用 worker_threads。',
        'worker_threads 与主进程同生共死（主进程崩全崩），cluster 的 Worker 是独立进程，隔离性更强。',
        '先用压测确认 CPU 瓶颈在 JS 计算而非 I/O，再动手并行化——多数"慢"是数据库和下游。',
      ],
    ),
  ]),
]);
