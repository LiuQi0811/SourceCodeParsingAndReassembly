requests>=2.32
beautifulsoup4>=4.12
lxml>=5.0
tqdm>=4.66
httpx>=0.27
flask>=3.0
# Playwright 需要额外下载浏览器内核，安装后执行：playwright install chromium
playwright>=1.49
# 可选（第 9 章「姿势二」）：用 Node 执行抠下来的 JS，需先安装 Node.js
PyExecJS>=2.7
# 第 10 章 AST 反混淆：纯 Python 的 JS 解析器（语义自检会自动调用 Node.js，可选）
esprima>=4.0
