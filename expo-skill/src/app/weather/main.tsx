import WeatherMainView from '@/components/view/weather/App';
/**
 * MainView component that serves as the main view for the weather application.
 * @author LiuQi
 * @returns  JSX.Element representing the main view of the weather application.
 */
export default function MainView() {
    return (<WeatherMainView />);
}